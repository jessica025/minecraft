# 1.1.0 发布验收记录

状态：1.1.0 基线版本已完成发布验收。此代码快照另含后续触控板调整，详见下节；最新调整尚未完成真实浏览器回归。线上部署尚未执行。

## 上传快照中的触控板调整

根据用户要求，触控板滑动及鼠标滚轮不再切换物品栏；使用数字键 1–8 或点击物品栏选择。最新 44/44 项单元与接口测试、TypeScript 检查和生产构建通过，浏览器用例已更新；本地浏览器进程受沙箱限制，未完成该调整的浏览器回归。下文 33 项浏览器测试和 30 分钟运行记录属于调整前基线，不作为此次调整已通过的证据。

## 已完成的修复

- 世界显示从每次编辑全量重建改为更新受影响的七个格子；实例容量按需增长，释放旧 GPU 实例对象。
- 准星使用网格射线遍历，保留水面高度与相邻面放置规则；攻击前更新相机和场景矩阵。
- 仪表复用 DOM，粒子最多 192 个，声音结束后断开音频节点。
- 存档版本/坐标/方块校验，基岩不可被编辑或旧存档破坏；读坏存档不覆盖，写失败明确反馈；导入导出备份与跨页面覆盖检测。
- 页面隐藏保存并暂停，鼠标锁定失败仍可操作，WebGL 初始化失败/设备丢失给出恢复提示。
- 向导取消旧请求，防止旧结果覆盖新问题，输入框 G 键不再误关闭；网络错误降级为本地建议。
- 服务端从开发依赖中分离；输入大小限制、超时、并发/频率限制、安全头、压缩、缓存、资源 404 和退出收尾。

## 已执行验证

- `npm run check`：44/44 项单元和接口测试通过、包含测试代码的严格类型检查通过、生产构建成功。原始记录见验证证据包中的 local-release-verification.log 与 node22-check.log。
- 几何：400 次确定性随机编辑后，增量显示与完整重建结果一致，并用独立暴露方块计数对照。
- 射线：300 条从世界外进入的随机射线，与 Three.js 实际网格交点/面/距离对照；另测六个方向、射程、水面、零向量。
- 模拟：10 个完整昼夜循环，夜怪上限、黎明清理与对象释放；移动、低帧率跳跃、台阶、墙、世界边界、体力、暂停、死亡重生。
- 服务：输入错误/过大/跨站、限流、并发、超时/错误/空提供者、健康检查、静态文件安全头、gzip 和缓存规则。
- `npm audit`：最终审计报告记录全依赖 0 个已知漏洞。审计结果仅代表查询时的软件公告状态。
- 精简安装：独立 staging 目录 `npm ci --omit=dev` 后启动生产 JS，确认 Vite/tsx/TypeScript/Playwright 均不存在；HTML、JS、health 返回 200，gzip 与 immutable 缓存正确。

## 性能测量

同一台机器、同一种子、同一代码执行器，去掉预热样本。CPU 微基准，不是 GPU 或整机 FPS 测量。

| 操作 | 原版中位数 | 优化后中位数 | 原版 P95 | 优化后 P95 | 样本 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 准星检测 | 0.502917 ms | 0.001250 ms | 0.607583 ms | 0.003083 ms | 1000 |
| 同格挖放 | 14.502583 ms | 0.002916 ms | 16.619334 ms | 0.003542 ms | 200 |

增量编辑收益与世界密度、是否发生容量扩展/灯光变化有关；不能把这里的倍数当作所有操作或整机帧率的承诺。

## 真实浏览器验收（2026-09-13 更新）

- Firefox 11/11、WebKit 11/11 通过，报告批次开始于 2026-09-13T13:18:40Z。
- Chromium 改为完整浏览器无头实现后 11/11 通过，报告批次开始于 2026-09-13T14:27:52Z；此前另一独立 Chromium 复测也为 11/11。
- 三浏览器合计 33 个场景通过；与 44 项单元/接口测试合计 77 项检查。各浏览器按不同批次完成，未声称同一批全绿。
- 已核对 Chromium、Firefox、WebKit 的日间和夜间截图，共 6 张，未发现明显界面裁切、重叠或黑屏。
- GPU 缓冲释放通过各浏览器五次世界切换测试；30 分钟持续运行结果另见下节。
- 对照候选包确认游戏、服务端和测试源码未变化；当前客户端产物 index-WnVDgf2e.js 的 SHA-256 为 06c2e9f67d203789d4120fbe53e7a1561dd97c80b5bf675619434eca57b0621f。
- Node.js 22.23.2 下 44 项测试与 TypeScript 检查通过；生产精简依赖环境启动、HTML/JS/health 200、gzip/缓存与正常退出均通过。此前 Node.js 24.11.1 环境亦通过。
- 最新全依赖审计为 0 个已知漏洞。

## 30 分钟稳定性验收（2026-09-14 核验）

- 原始报告状态 passed；实际计时 1,800,004 ms，60/60 个采样窗口，日志对应完整，页面错误 0。
- Chromium 145.0.7632.6，Node.js 24.11.0；运行时间为北京时间 2026-09-13 22:53:38 至 23:23:44（含启动及收尾）。客户端 SHA-256 与当前候选产物一致。
- 30 秒窗口帧间隔中位数的中位数为 8.3 ms；各窗口 P95 最高约 10.2 ms。这是本机无头浏览器 requestAnimationFrame 计时，不是跨设备帧率保证。
- 场景经过约 5 次昼夜循环及 5 次退出重入；运行中 GPU 缓冲数为 231–303，退出后为 10（初始菜单为 5），通过既定基线 +8 阈值，不代表严格零资源残留。
- 最终存档版本 1，记录游戏时间约 1,800.77 秒；音频上下文 running，检测到一次发声节点启动。最终截图已查看，界面正常。
- 本次角色位置固定、编辑数为 0，鼠标未锁定；这份自动报告不覆盖真人体验；用户已另行确认转向、连续挖放和音效正常。
- 原始证据：30分钟稳定性结果.json、30分钟稳定性验证.log、30分钟稳定性最终画面.png。

## 真人验收与发布结论

2026-09-14，用户针对鼠标转向、连续挖放和音效的验收问题明确回复“正常”。计划内验收项目全部完成，放行默认本地向导版本。该反馈确认操作体验，不等同于测量原生鼠标锁定 API 的状态。

若选择 Docker、公网入口或在线 Bedrock 向导，须在所选部署环境验证镜像、TLS/代理与实际账户权限。这些是可选部署方式的环境检查；本次未执行线上部署。

## 复现常规验收

```bash
npm ci
npx playwright install --with-deps chromium firefox webkit
npm run release:check
```

Chromium 使用完整浏览器的无头实现。无 GPU 的隔离测试环境可显式设置 PLAYWRIGHT_SOFTWARE_GL=1 使用 SwiftShader；软件渲染结果不能替代目标机器的性能测量。

保留每次 JSON 报告、日志、截图和失败 trace。本次已依据持续运行报告和用户体验确认更新放行状态和发布包。不要把常规回归通过或启动成功当作线上部署完成。
